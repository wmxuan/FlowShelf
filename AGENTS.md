# FlowShelf Agent Guide

## 项目定位

FlowShelf 是一款 AI 原生的"个人数字资产管家"，通过浏览器扩展 + Web 应用的形态，帮助用户管理三类数字资产：

1. **知识**（待学 → 沉淀）：从阅读到知识库的完整链路
2. **工具**（收藏 → 复用）：永不丢失的工具箱
3. **临时**（Tab → 流转）：浏览器标签的高效治理

### 一句话定位

> 你的 AI 数字信息管家——从信息洪流中沉淀个人知识资产，永不丢失常用工具。

### 当前阶段

- **状态**：MVP 立项，PRD v1.0 已完成
- **下一步**：进入 M0 原型验证阶段（用户访谈 + Figma 高保真原型）
- **尚未开始编码**：当前处于产品定义期，技术脚手架待搭建

## 文档索引

所有产品文档位于 `docs/` 目录：

| 文档 | 说明 |
|---|---|
| [FlowShelf_PRD_v1.0.md](docs/FlowShelf_PRD_v1.0.md) | 完整产品需求文档（14 章 + 2 附录），含功能设计、线框图、AI 能力、数据模型、开发计划 |
| [FlowShelf_对话记录_v1.0.md](docs/FlowShelf_对话记录_v1.0.md) | 8 轮需求讨论演进记录 + 8 条关键决策 + 后续行动项 |

**新对话恢复上下文的方式**：先读本文件的"核心架构"与"关键决策记录"章节，再按需读取 `docs/` 下的详细文档。

## 核心架构：三类内容池模型

FlowShelf 的核心设计是"三类内容池"，每个池有明确的边界规则：

```
┌──────────────────────────────────────────────────────────┐
│                  FlowShelf 数字资产管理                    │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  待学习队列  │  │   工具箱     │  │  知识卡片库  │    │
│  │  (知识·未读) │  │  (工具·复用) │  │  (知识·沉淀) │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
│         │                                  ▲              │
│         └────  价值门槛（读+生成卡片） ─────┘              │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │           浏览器扩展（统一入口）                    │  │
│  │  Tab 管理 · 智能收藏分流 · 待学习队列 · 工具箱入口  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │           AI 引擎（贯穿全产品）                     │  │
│  │  归组 / 分类 / 标签 / 摘要 / 检索 / 聚类 / 推荐     │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 内容池边界规则

| 内容池 | 内容性质 | 流动方向 | 价值门槛 | 检索方式 |
|---|---|---|---|---|
| **待学习队列** | 知识型·待读 | 流向知识库 | 读+生成卡片才晋升 | 任务式回溯 + 过期提醒 |
| **工具箱** | 工具/娱乐·反复用 | 不流动（终点站） | 无门槛 | 语义 + 多标签 + 频率 + 时间 |
| **知识卡片库** | 知识型·已沉淀 | 不流动（终点站） | 已读+AI总结 | 语义 + 关联推荐 + 知识图谱 |

### 三大设计原则

1. **价值门槛**：只有"已读+生成卡片"的内容才进入知识库，保证知识库高信噪比
2. **终点站原则**：工具箱和知识库都是终点，内容不互相流动，避免污染
3. **零整理负担**：所有内容池的索引、标签、聚类均由 AI 自动完成，用户只做"确认/修正"

## 关键决策记录

以下决策在需求讨论中确定，是产品设计的基石，**后续讨论与开发不得违背**：

| # | 决策 | 理由 |
|---|---|---|
| 1 | 聚焦构思 5/1/2 整合 MVP（文章卡片+Tab管理+工具箱） | RICE 评分最高，全链路协同 |
| 2 | 浏览器扩展 + Web 应用双形态 | 扩展做入口，Web 做沉淀；移动端延后 |
| 3 | 拆分"待学习队列"与"知识卡片库" | 未读内容不污染知识库；收藏夹从 Web 大本营剥离 |
| 4 | "待学习"命名（副标题"稍后读"） | 目标感强 + 降低压力；替代"收藏夹"概念 |
| 5 | 新增"工具箱"作为第三类内容池 | 工具类网页不需沉淀为知识，覆盖完整数字资产类型 |
| 6 | 工具箱抛弃文件夹，用多标签+语义检索 | 文件夹模型结构性失败；核心差异化 |
| 7 | 价值门槛：读+生成卡片才进知识库 | 保证知识库高信噪比；与 Cubox/Pocket 本质差异 |
| 8 | AI 全自动索引，零整理负担 | AI 时代的根本性突破；用户全程只做"确认/修正" |

## 产品形态

| 形态 | 角色 | 用户何时用 |
|---|---|---|
| **浏览器扩展**（Chrome/Edge MV3） | 流量入口 + 即时操作 | 浏览网页时随手用 |
| **Web 应用**（flowshelf.app） | 知识大本营 + 深度整理 | 回顾、搜索、整理时用 |

移动端 MVP 阶段用响应式 Web 兜底，不做原生 App。

## 技术栈

- 前端：Next.js、React、TypeScript、Ant Design、TanStack Query
- 浏览器扩展：Chrome MV3、Plasmo 框架
- 后端：Python、FastAPI、Pydantic、SQLAlchemy、Alembic
- 数据库：PostgreSQL、pgvector（语义检索）
- AI：OpenAI GPT-4o-mini + text-embedding-3-small，DEMO_MODE 可切换通义千问
- 测试：Pytest、Playwright
- 运行环境：Docker、Docker Compose

## 目录规范（推荐）

```text
flowshelf-extension/        # 浏览器扩展（独立 npm 包）
  src/
  background/
  content/
  popup/
  options/

frontend/                   # Web 应用
  app/
  components/
  features/
    cards/                  # 知识卡片库
    toolbox/                # 工具箱
    learning-queue/         # 待学习队列
    weekly-report/          # 周报回顾
  hooks/
  services/
  stores/
  types/

backend/
  app/
    api/
    agents/                 # AI 引擎
    core/
    db/
    models/
    prompts/                # Prompt 文件独立保存
    providers/              # LLM/Embedding provider 抽象
    schemas/
    services/
    tools/
  alembic/
  tests/

docs/
```

目录约束：

- `frontend/features/` 按业务域拆分（cards / toolbox / learning-queue），不按页面杂糅
- `backend/app/api/` 仅处理请求与响应，不承载核心业务逻辑
- `backend/app/services/` 承载应用服务和编排逻辑
- `backend/app/providers/` 封装真实模式与 Demo 模式下的 LLM、Embedding 能力
- `backend/app/prompts/` 独立保存 Prompt 文件，禁止将核心 Prompt 硬编码进业务代码

## 编码规范

通用原则：

- 保持类型完整：前端使用 TypeScript，后端使用 Pydantic 和类型注解
- 优先小步提交、分阶段可运行，不一次性铺开全部代码
- 所有外部调用均通过抽象接口访问，不在业务层写死供应商细节
- 关键代码可加简短注释，但避免无意义注释

前端规范：

- 使用 App Router
- 服务端数据获取统一通过 TanStack Query 封装
- 页面状态与后端状态分离

后端规范：

- API 层 → Service 层 → Provider / Tool 层分层清晰
- 所有大模型结构化输出必须经过 Pydantic 校验
- 所有数据库变更都必须生成 Alembic 迁移
- 统一错误响应格式

## AI Provider 策略

- 主模型：OpenAI GPT-4o-mini（摘要、标签、分类）
- 嵌入模型：text-embedding-3-small（语义检索）
- 备选：通义千问（DEMO_MODE 切换）
- 所有 AI 输出必须经过 Pydantic 校验（结构化输出）
- DEMO_MODE 与真实模式必须走统一业务流程，只切换底层 provider

## 核心 AI 能力清单

| 能力 | 触发时机 |
|---|---|
| Tab 归组 | 用户打开扩展 |
| 网页类型识别（工具/文章/视频） | 收藏时 |
| 多标签自动抽取（3-5 个） | 收藏时 |
| 内容摘要生成（100-200 字） | 生成卡片时 |
| 关键观点抽取（3-5 条） | 生成卡片时 |
| 语义检索（自然语言 → 匹配） | 用户搜索时 |
| 关联推荐（卡片相似度） | 查看卡片时 |
| 智能聚类（发现收藏分组） | 每周/触发式 |
| 行为驱动排序（访问频率+时间衰减） | 实时计算 |

## 里程碑

| 里程碑 | 时间 | 交付物 | 验收标准 |
|---|---|---|---|
| M0 原型验证 | 第 4 周 | Figma 高保真原型 + 30 位用户访谈 | 痛点验证通过率 ≥ 70% |
| M1 MVP Alpha | 第 8 周 | 扩展 + Web（卡片库+工具箱+待学习基础版） | 内测 50 人，7 日留存 ≥ 30% |
| M2 MVP Beta | 第 12 周 | 公测版 + AI 全能力 | 注册→首次卡片转化 ≥ 50% |
| M3 付费版上线 | 第 16 周 | Pro 订阅 + 知识图谱 v1 | 付费转化 ≥ 3% |
| M4 增长 | 第 24 周 | 移动端 + 关联推荐 + 增长机制 | DAU 5,000+，NPS ≥ 40 |

## 测试命令（待项目脚手架搭建后补充）

- 后端单元测试：`pytest backend/tests`
- 前端端到端测试：`playwright test`
- 数据库迁移：`alembic upgrade head`
- 本地启动：`docker compose up --build`

## 禁止事项

本项目本期明确禁止：

- 引入微服务、Kubernetes
- 引入 Kafka、Redis、Celery 等额外基础设施作为默认方案
- 引入多 Agent 协作
- 引入知识图谱（v2 阶段才考虑）
- 引入模型微调
- 引入多个向量数据库
- 在仓库中提交 API Key 或其他敏感凭证
- 在工具箱中引入文件夹模型（已决策用多标签替代）
- 让未读内容进入知识卡片库（已决策设价值门槛）

## 开发原则

- 优先实现 Demo 主闭环（Tab → 待学习 → 卡片），不追求一次实现所有增强能力
- `DEMO_MODE=true` 与 `DEMO_MODE=false` 必须走统一业务流程，只切换底层 provider
- 所有知识卡片、工具收藏、待学习项都必须可追溯来源
- 无依据时必须明确标记资料不足，不得编造来源
- 单个模块失败不能拖垮整条链路（尤其是 AI 调用和网页解析）
- 每阶段必须独立可运行、可验收、可演示

## 后续行动项

| # | 行动项 | 阶段 |
|---|---|---|
| 1 | 基于 PRD 拆解 MVP 详细功能清单与数据库表结构 | M0 |
| 2 | 输出第一阶段用户访谈大纲（30 位用户） | M0 |
| 3 | 撰写关键 AI Prompt 设计草案（摘要、标签、分流） | M0 |
| 4 | Figma 高保真原型设计 | M0 |
| 5 | 技术选型确认与项目脚手架搭建 | M1 |
| 6 | 浏览器扩展开发（Tab 管理 + 收藏分流 + 待学习） | M1 |
| 7 | Web 应用开发（卡片库 + 工具箱） | M1 |
| 8 | AI 引擎开发（Prompt + 索引 + 检索） | M1 |
