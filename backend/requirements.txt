# FlowShelf 完整版依赖（含本地嵌入模型 sentence-transformers）
# 打包发布时：默认用 requirements-base.txt（体积小 80%）
# 用户需要本地嵌入时，运行：pip install sentence-transformers>=3.0.0
#
# 体积分析：
#   基础依赖：约 200-300MB
#   + sentence-transformers：引入 torch(2.2GB+) + transformers(100MB+)
#
# 因此发布包仅包含基础依赖 + openai embedding 模式，
# sentence-transformers 作为运行时可选依赖，由用户自行安装。
-r requirements-base.txt

# 【可选】本地嵌入模型（体积 ~2.3GB）：
# sentence-transformers>=3.0.0
